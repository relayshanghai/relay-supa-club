import { Layout } from 'src/components/layout';
import { AccountPage } from 'src/components/account/account-page';

const Page = () => {
    return (
        <Layout>
            <AccountPage />
        </Layout>
    );
};

export default Page;
