import { Modal } from '../modal';

export const CreateSequenceModal = (props: any) => {
    return (
        <Modal {...props}>
            <div>Create Sequence Modal</div>
        </Modal>
    );
};
