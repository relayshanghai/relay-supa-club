export const insertInfluencer = (_insert: any) => {
    // save to data store
    return { id: 0 };
};

export const insertInfluencerProfile = (_insert: any) => {
    // save to data store
    return { id: 0 };
};
