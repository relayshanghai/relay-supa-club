export const LANDING_PAGE = (details: string): string => {
    return `Landing Page, ${details}`;
};

export const SIGNUP_WIZARD = (details: string): string => {
    return `Signup Wizard, ${details}`;
};

export const AI_EMAIL_GENERATOR = (details: string): string => {
    return `AI Email Generator, ${details}`;
};
